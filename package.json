{
  "name": "coherence-mcp",
  "version": "0.1.0",
  "description": "MCP server that surfaces coherence, governance, and safety primitives: Wave/Bump validation, ATOM trail + gates, .context.yaml packing, AWI intent scaffolding, and docs/search across the SpiralSafe corpus.",
  "type": "module",
  "bin": {
    "coherence-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "bench": "python3 scripts/benchmark.py",
    "bench:chaos": "python3 scripts/benchmark.py --chaos-mode"
  },
  "keywords": [
    "mcp",
    "coherence",
    "governance",
    "safety"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
